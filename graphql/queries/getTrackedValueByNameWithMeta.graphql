query getTrackedValueByNameWithMeta(
  $name: String!
) {
  trackedValue(name: $name) {
    name
    unit
    displayType
    plotColor
    plotDash
    value
    lastUpdate
    lastSent
    minUpdateInterval
    history
    maxHistoryLength
  }
}
